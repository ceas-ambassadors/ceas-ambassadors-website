extends ../layout
//- load member list style
include ./mixins/list.pug
include ./mixins/table.pug

block content
  +member-list(members, "Members")
  if(user && user.super_user)
    +member-table(members, "Hours")
